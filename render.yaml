services:
  - type: web
    name: fastapi-file-processor
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: |
      echo "Using port: $PORT" && uvicorn one_SEE_render:app --host 0.0.0.0 --port $PORT
